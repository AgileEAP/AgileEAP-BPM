(function (window, undefined) {
    if (window.EFORM) {
        return;
    }
    if (!window.EFORM) {
        window.EFORM = (function () {
            var EFORM =
                {

                };
        })();
    }
})(window);